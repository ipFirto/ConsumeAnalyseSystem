# Frontend API base URL used in production build.
# Example:
# VITE_API_BASE_URL=https://api.your-domain.com
#
# If left empty, frontend requests will use same-origin (recommended with Nginx reverse proxy).
VITE_API_BASE_URL=
